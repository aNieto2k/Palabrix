{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node ./generate-version.js && vite build",
    "preview": "npx serve ./dist -l 3001",
    "test": "vitest run",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "jsdom": "^26.1.0",
    "vite": "^7.0.4",
    "vitest": "^1.0.0"
  }
}
